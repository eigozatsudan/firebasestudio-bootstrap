services:
  frontend:
    build: ./frontend
    volumes:
      - ./frontend:/app
    ports:
      - "${PORT:-5173}:5173"
  backend:
    build: ./backend
    volumes:
      - ./backend:/app
    ports:
      - "8000:8000"
